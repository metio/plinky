{
    "name": "plinky-worker",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "description": "Plinky's optional backend. See BACKEND.md at the repository root.",
    "license": "AGPL-3.0-or-later",
    "scripts": {
        "typecheck": "tsc",
        "test": "vitest run",
        "deploy": "wrangler deploy",
        "dev": "wrangler dev"
    },
    "devDependencies": {
        "@cloudflare/vitest-pool-workers": "^0.22.0",
        "@cloudflare/workers-types": "^5.20260826.1",
        "typescript": "^6.0.3",
        "vitest": "^4.1.9",
        "wrangler": "^4.126.0"
    }
}
