# SPDX-FileCopyrightText: The Plinky Authors
# SPDX-License-Identifier: AGPL-3.0-or-later
# Rules live in .markdownlint.yaml; this carries the cli2-only ignore so a local
# or dev-container run does not lint dependencies' bundled Markdown.
ignores:
  - node_modules
